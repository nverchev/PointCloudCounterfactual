defaults:
  - encoder: default_encoder
  - decoder: default_decoder

head: VQVAE
book_size: 16
embedding_dim: 4
w_dim: 256
z_dim: 32
vq_ema_update: false
vq_noise: 0.
double_encoding: false
output_points: ${data.input_points}
name: 'Autoencoder'