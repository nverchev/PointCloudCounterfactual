study_name:
  embedding

params:

  autoencoder.architecture.vq_noise:
    suggest: suggest_float
    settings:
      low: 0.1
      high: 10

  autoencoder.objective.c_embedding:
    suggest: suggest_float
    settings:
      low: 0.1
      high: 10
