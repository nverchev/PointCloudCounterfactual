study_name:
  w_encoder

n_trials: 300

params:

  autoencoder.architecture.encoder.w_encoder.proj_dim:
    suggest: suggest_categorical
    settings:
      choices: [256, 512]


  autoencoder.architecture.encoder.w_encoder.n_heads:
    suggest: suggest_categorical
    settings:
      choices: [4, 8, 16]

#  autoencoder.architecture.encoder.w_encoder.hidden_dims_conv:
#    suggest: suggest_list
#    settings:
#      min_length: 1
#      max_length: 3
#      suggest: suggest_int
#      settings:
#        low: 4
#        high: 16
#        log: true


  autoencoder.architecture.encoder.w_encoder.hidden_dims_lin:
    suggest: suggest_list
    settings:
      min_length: 1
      max_length: 4
      suggest: suggest_int
      settings:
        low: 128
        high: 1024
        log: true
