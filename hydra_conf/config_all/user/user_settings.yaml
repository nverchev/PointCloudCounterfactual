cuda: true
seed:
generate:
  batch_size: 1
  bias_dim: 0
  bias_value: 1.
path: {}
    # exp_par_dir:
    # metadata_dir:
    # data_dir:
plot:
    interactive: false
    indices_to_reconstruct: [0, 1, 2, 3, 4]
    double_encoding: false
counterfactual_value: 1

load_checkpoint: 0
checkpoint_every: 100
n_generated_output_points: ${autoencoder.model.training_output_points}